dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "tools,platform-tools,extra-android-support,extra-google-m2repository,extra-google-google_play_services"
    - echo y | android update sdk --no-ui --all --filter "android-25,build-tools-25.0.0"